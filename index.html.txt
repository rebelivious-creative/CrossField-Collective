<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crossfield Collective | Operation-Led Approach</title>
    <style>
        :root {
            --bg-color: #030303;
            --text-main: #ffffff;
            --text-muted: #888888;
            --accent: #222222;
        }

        body { 
            margin: 0; 
            font-family: 'Helvetica Neue', Arial, sans-serif; 
            background-color: var(--bg-color); 
            color: var(--text-main); 
            overflow-x: hidden; 
            line-height: 1.6;
        }
        
        /* Navigation */
        header { 
            padding: 30px 5vw; 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            position: fixed;
            top: 0; width: 90vw;
            background: rgba(3, 3, 3, 0.8);
            backdrop-filter: blur(10px);
            z-index: 100;
        }
        .logo { font-weight: 800; letter-spacing: 2px; font-size: 1.2rem; text-transform: uppercase;}
        nav a { 
            color: var(--text-main); 
            text-decoration: none; 
            margin-left: 30px; 
            font-size: 0.9rem;
            letter-spacing: 1px;
            transition: color 0.3s ease;
        }
        nav a:hover { color: var(--text-muted); }

        /* Hero Section */
        .hero { 
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center; 
            padding: 0 5vw;
            perspective: 1000px;
        }
        h1 { 
            font-size: clamp(3rem, 6vw, 6rem); 
            margin-bottom: 20px; 
            text-transform: uppercase; 
            line-height: 1.1;
            letter-spacing: -2px;
            transition: transform 0.1s ease-out;
        }
        .hero-sub { 
            color: var(--text-muted); 
            font-size: 1.2rem; 
            max-width: 600px;
        }

        /* Infinite Marquee */
        .marquee-container {
            width: 100%;
            overflow: hidden;
            background: var(--text-main);
            color: var(--bg-color);
            padding: 15px 0;
            display: flex;
            white-space: nowrap;
            font-weight: bold;
        }
        .marquee-content {
            display: inline-block;
            animation: scroll 15s linear infinite;
            font-size: 1.2rem;
            letter-spacing: 2px;
            text-transform: uppercase;
        }
        .marquee-content span { padding: 0 40px; }
        @keyframes scroll {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        /* Content Section */
        .section {
            padding: 100px 5vw;
        }
        .section-title {
            font-size: 2.5rem;
            margin-bottom: 40px;
            text-transform: uppercase;
            border-bottom: 1px solid var(--accent);
            padding-bottom: 20px;
        }
        .intro-text {
            font-size: 1.5rem;
            color: var(--text-muted);
            max-width: 800px;
            margin-bottom: 60px;
        }

        /* Interactive Grid */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }
        .card {
            background: #0a0a0a;
            padding: 50px 40px;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
            position: relative;
            border: 1px solid var(--accent);
            overflow: hidden;
        }
        .card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(45deg, transparent, rgba(255,255,255,0.03), transparent);
            transform: translateX(-100%);
            transition: transform 0.6s;
        }
        .card:hover::before { transform: translateX(100%); }
        .card:hover {
            transform: translateY(-10px);
            background: #111;
            border-color: #333;
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
        }
        .card h3 { 
            margin-top: 0; 
            font-size: 1.5rem; 
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .card p { 
            color: var(--text-muted); 
            opacity: 0.8; 
            transition: opacity 0.3s; 
            margin-bottom: 0;
        }
        .card:hover p { opacity: 1; color: #ccc;}

        /* Footer */
        footer {
            padding: 50px 5vw;
            text-align: center;
            border-top: 1px solid var(--accent);
            color: var(--text-muted);
            font-size: 0.9rem;
        }
        .cta-btn {
            display: inline-block;
            margin-top: 30px;
            padding: 15px 40px;
            background: var(--text-main);
            color: var(--bg-color);
            text-decoration: none;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: transform 0.3s, background 0.3s;
        }
        .cta-btn:hover {
            transform: scale(1.05);
            background: #ccc;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">Crossfield Collective</div>
        <nav>
            <a href="#about">About</a>
            <a href="#solutions">Solutions</a>
            <a href="#contact">Direct Line</a>
        </nav>
    </header>

    <section class="hero">
        <h1 id="dynamic-text">Design to support execution,<br>not just ideas.</h1>
        <p class="hero-sub">An Operation-Led Approach bridging the JB-SG Corridor.</p>
    </section>

    <div class="marquee-container">
        <div class="marquee-content">
            <span>Real operators</span> • <span>Practical automation</span> • <span>Designed for SMEs</span> • <span>JB-SG Corridor</span> • 
            <span>Real operators</span> • <span>Practical automation</span> • <span>Designed for SMEs</span> • <span>JB-SG Corridor</span> •
        </div>
    </div>

    <section id="about" class="section">
        <h2 class="section-title">Experience-Driven. Systems-Led.</h2>
        <p class="intro-text">
            Bridging the gap between operational chaos and digital precision. We treat branding as an operational asset, delivering agency-grade visual identities engineered to integrate seamlessly with your automation.
        </p>

        <div class="grid-container" id="solutions">
            <div class="card">
                <h3>Direct Accountability</h3>
                <p>No junior staff. Every project is architected and executed by the Lead Practitioner.</p>
            </div>
            <div class="card">
                <h3>Practical Innovation</h3>
                <p>We only deploy technology that solves a specific operational bottleneck—never for the sake of the "hype."</p>
            </div>
            <div class="card">
                <h3>Intelligent Systems</h3>
                <p>From sourcing a corporate venue to automating customer inquiries, we provide the framework to scale.</p>
            </div>
            <div class="card">
                <h3>High-Value Growth</h3>
                <p>By stripping away agency overhead, we empower service professionals to reclaim their time and focus exclusively on growth.</p>
            </div>
        </div>
    </section>

    <footer id="contact">
        <h2>Experience the system in action.</h2>
        <a href="#" class="cta-btn">Direct Line</a>
        <p style="margin-top: 50px;">Crossfield Collective. Real operators. Practical automation. Designed for SMEs.</p>
    </footer>

    <script>
        // Hero Text Tilt Interaction (follows the mouse cursor)
        const heroText = document.getElementById('dynamic-text');
        
        document.addEventListener('mousemove', (e) => {
            let xAxis = (window.innerWidth / 2 - e.pageX) / 50;
            let yAxis = (window.innerHeight / 2 - e.pageY) / 50;
            heroText.style.transform = `rotateY(${xAxis}deg) rotateX(${yAxis}deg)`;
        });

        // Smooth Scroll for Navigation Links
        document.querySelectorAll('nav a').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>

</body>
</html>